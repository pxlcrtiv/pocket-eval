"""pocket-eval — the LLM eval harness that fits in your pocket.

CPU-only, keyless, offline evaluation of small language models:
perplexity on a bundled corpus + multiple-choice QA scoring, with a
deterministic n-gram baseline that runs everywhere (CI included) and an
optional Hugging Face backend for real tiny models.
"""

__version__ = "0.1.0"